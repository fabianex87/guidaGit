/**
 * Su github
 * Setting/Branches/Add rule/master(digitare su branch name pattern)
 *
 * Protect matching branches> Require pull request reviews before merging
 * (cioè leggere il codice prima di fare il merge)
 *
 */
